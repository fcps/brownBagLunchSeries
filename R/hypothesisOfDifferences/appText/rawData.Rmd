# Viewing the raw data
* Explain that these data are simulated based on the values that they provide using a Gaussian distribution (you can reference that this is what people actually mean when saying "Bell Curve")
* The table is sortable and searchable so they can look through different things fairly easily
